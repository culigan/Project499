<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ExpenseTracker"
             x:Class="ExpenseTracker.MainPage"
             BackgroundColor="Blue">

    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="Expense Tracker!" 
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand" />
        <Label Text="{Binding ValueTemp}"
               HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand" />
        <ListView ItemsSource="{Binding ItemList}" RowHeight="30" BackgroundColor="White">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding TestValue1}"></Label>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </StackLayout>

</ContentPage>
