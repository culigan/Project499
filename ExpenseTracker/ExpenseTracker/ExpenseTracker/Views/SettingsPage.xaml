<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ExpenseTracker.Views.SettingsPage"
             x:Name="Settings">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical" Padding="15,0,15,0">
            <Label Text="Transaction Start Date" />
            <DatePicker Date="{ Binding PickerStartDate}" HorizontalOptions="Start"/>
            <Label Text="Transaction End Date" />
            <DatePicker Date="{ Binding PickerEndDate}" HorizontalOptions="Start" />
            <Label Text="Number of Transactions Displayed"/>
            <Entry Placeholder="100" Text="{Binding NumberTrans, Mode=TwoWay}" Keyboard="Numeric"/>
            <Button Text="Save Settings" Clicked="OnSave"/>
            <Button Text="Cancel" Clicked="OnCancel"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>